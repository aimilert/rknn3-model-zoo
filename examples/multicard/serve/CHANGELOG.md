# 变更记录（板端服务栈）

这份文件只干一件事：把 `examples/multicard/serve/` 这套服务栈**加了什么功能、修了哪些坑**按时间
列清楚。**解释不写在这里**——每条只留一行「症状 → 根因」，要细节去下面这几个地方：

- 完整论述与实测数据：`../PP流水线优化实验/多Session并发推理方案.md`（下称**方案文档**，括号里的 `§x.y` 就是它的小节号）
- 怎么给人演示：`DEMO.md`
- 接口与起服务：`README.md`

图例：🆕 新功能 · 🐞 修 bug · ⚠️ **口径变了**（会让之前量到的数字失去可比性，引用前先看这条） · 📄 只动文档。
每条后面的 `\`hash\`` 是引入它的提交，要考古就 `git show`。

---

## 2026-09-18

### ⚠️ 演示页的"聚合 tok/s"口径改了：四路长度不齐时它会凭空变小

**症状**：一次真实四路（网页）屏幕上写着

```
4 路并发：合计 2095 tok / 墙钟 135.7s → 聚合 15.43 tok/s
9.83 / 10.07 / 11.80 / 9.72 tok/s ……（四个面板各自的速率）
```

四个面板**每路都是全速**（9.8~11.8，单路基线约 10.6），可顶上那个数看着跟单路是一个量级，
读出来就是"四路并发没什么用"。

**根因不是算错，是口径**：那 135.7s 里只有前 ~18 秒是四路在跑——**有一路 1602 token 跑了
135.7s，另外三路约 16s 就收工了**，剩下 120 秒**只有一路**在跑。`Σtok / 墙钟` 把"一路的
吞吐"摊进了"四路并发"的分母，于是：

- **各路速率和** `Σ(tok_i/el_i)` = **41.42 tok/s** ← 四路同时跑时整机的总产出
- **墙钟摊薄** `Σtok/墙钟` = **15.43 tok/s** ← 平均只有 **1.37/4** 路在跑的那个数
- 两者差 **2.7 倍**，不是误差，是**两个不同的问题**

**改法**（`demo_4chat.html` 的 `summarize()`）：

| | 口径 | 用在哪 |
|---|---|---|
| 各路速率和 | `Σ(tok_i/el_i)`，**与单路基线同口径** | 横幅**主数**；**伸缩比只用它**（41.36/10.63 = 3.89×，用摊薄值会算出 1.45×） |
| 墙钟摊薄 | `Σtok_i/墙钟` | 跟在后面，且**只在四路长度齐时才有意义** |

长度不齐时（`Σel/墙钟 < n-0.5`）横幅自己标出来：`四路长短不齐，平均只有 1.37/4 路在跑，
墙钟摊薄 15.44 tok/s（空转的路也算进了分母，别当并发性能看）`；排队警告移到整句**最后**
（它撤的就是刚印出来的伸缩比）。

**顺带修了一个检查的盲区**：`page_check.js` 以前是**自己** `panels.map(send)` 发四路，
页面里 `summarize + setSum` 那两句压根不执行 ⇒ **汇总行怎么算的没有任何检查覆盖**。
现在把「②」的处理器提成 `runAll()`，检查直接调它（`lastRound()` 取回结果），
并核对页面上真写出来的那句。三个变异体（伸缩比改用摊薄值 / 稀释警告永不触发 /
把摊薄当主数印）都被抓住。

⏳ 哈希待回填（本条的收尾提交）

### 🆕 演示页 UI/UX 改版：状态徽标、面板头性能小字、多行自适应输入框

**改了什么**：顶部「待机」→ **带指示灯的徽标**（`idle` 绿 / `busy` 琥珀闪 / `wait` 蓝闪 /
`err` 红，灯是 CSS 画的、不是 emoji），右侧控件收成一组（`flex`、垂直居中、间距 16px），
按钮分三级（主=亮蓝实心 / 次=描边透明 / 危险=深灰 hover 变红），数字框自绘边框+内阴影，
**面板头右侧新增 TTFT + tok/s 的灰色小字**，面板内容加内边距，
单行输入框换成**高度随内容自适应的 textarea**，发送键**内嵌**在框的右下角。

**两处"屏幕上会骗人"的地方顺手堵上**：徽标**必须字和色一起换**（同一个灰灯下"生成中"和
"待机"看不出区别，而颜色恰恰是隔投影仪唯一看得出的东西）；**新一轮开始时面板头那格必须
清空**（挂着上一轮的首字延迟，在最该判断"到底开始了没有"的那一帧上给的是旧数字）。
回车分流：**Enter 发送、Shift+Enter 换行**。

**没照原话做的三处**：不用 Tailwind CDN（这一页是板卡**离线**发出来的，引 CDN = 断网时
打不开页面）、灯用 CSS 而非 🟢🟡（emoji 跨平台不一致、投影可能是方框）、
面板头那格**只放 TTFT + tok/s**（底部统计行是记账，看不见首字延迟）。

**验的边界**：`page_check.js` 能守住的只有"性能小字写出来了""新一轮那格是空的"两条断言
（元素被删/`data-role` 拼错时，页面照跑照出答案、只是那格永远空着）；
**配色与间距没有断言**，只靠无头浏览器截图看过（本机桩 + 真板卡各一张，真机那张是
**运行中**一帧）。**没在窄窗口/触摸屏上试过**。方案文档 §9.15。

⚠️ 页面是 `_static_demo` **每次请求现读磁盘**发的 ⇒ **只换文件、不重启网关**
（省掉 ~240s 模型重载）。旧件留 `serve/bak_20260918_ui/`。`bd49947`

### 🐞 NPU 空着，用户却在干等：第 N+1 段对话得等到某一段静默满 `IDLE_TTL`

**症状**：网页某一格 `158 tok · 100.9s · 1.57 tok/s`，而那一刻 **NPU 上一个请求都没在跑**。

**根因**：会话池只有一条回收阈值 `IDLE_TTL`（300s），且 `_reap_idle_locked` **只在 `acquire`
里被调用**。于是 N 段对话把 N 个会话**绑着不用**时，第 N+1 段对话要一直等到某一段**静默满 300s**
才拿得到会话——这中间 NPU 全空。板上实测（`page_repro.py`，四段对话静默 70–90s）：第 5 段
**排队 226.0s**，而它拿到会话的时刻正是 `web-1` 跨过 300s 的那一刻。`1.57 tok/s` 是**端到端**的
数——页面把这段排队也算进分母了，屏幕上分不出"模型慢"和"根本没轮到它"。

**修法**（两层，第二层只在第一层不够用时降下来）：

| 层 | 阈值 | 什么时候用 | 收几段 |
|---|---|---|---|
| 一（原有） | `IDLE_TTL` 300s | 只要有人问就一直在跑 | 所有静默超阈值的 |
| 二（新，`--contend-idle`） | 15s | **只有**一个空闲会话都找不到、这一轮非等不可 | **只收最闲的那一段** |

`idle_ttl=0` 仍是**总开关**：第二层绕过它就等于把"0 = 不回收"这条契约作废，而回归套件正是靠
它来证明自己真的在主动 `close`（不是碰巧被回收救了）。

**顺带把"等"变成可见的**：`X-KV-Reuse` 加 `wait=`、日志 `lease:` 行排过队就打 `排队 X.Xs`、
`/v1/pool` 报 `contend_idle_s`、页面排过队时行内显示 `⏳ 等待空闲会话…`、统计行拆成
`（其中排队 X.Xs，实际推理 Y.Ys）`、四格汇总栏加"有 N 路排过队 → 伸缩比不可引用"。
**不这么做就没法自证**：排队在 tok/s 上只表现为"变慢"，看不出异常，而那个 tok/s 恰恰是被排队污染的。

**板上实测（2026-09-18）**：

| | 四段对话静默 | 第 5 段对话 |
|---|---|---|
| 改前 | 70–90s | **排队 226.0s**（等满 `IDLE_TTL`） |
| 改后（在静默 0.5s 时敲） | 0.5s | 进队列，**15.4s** 拿到会话（`wait=15.0`） |
| 改后（在静默 24–42s 时敲） | >15s | **没进队列**，`wait=0.0`、TTFT 1.17s |

`reaped_total` 精确地 0→1→2（一次只收一段）；静默 41.8s 的两段**不被收**——只在真有人敲门时才动。
日志长这样：`[pool] 会话回收：id:web-3 静默（有人排队） 73.0s > 15.0s，s3 交还给排队者`、
`[gateway] lease: session=0 reset=True sent=204/204 bytes (base=0) 排队 15.0s`。
⚠️ 改的是**网关进程**，必须重启（模型重载 ~240s）；旧件留 `serve/bak_20260918_contend/`。`b5483bd`

### 🐞 关思考时，网页某一格会出现满屏英文推理

**症状**：`demo_4chat.html` 的四个对话框里有一格不回答问题，而是吐一整屏
`<think>Thinking Process:…`，其他三格正常。**同一块板、同一份模型、同一个网关**。

**根因不在页面，在 prompt 的最后一个字节**：关思考一直靠 `/no_think` 这个**软**提示实现，
而"要组织语言"的题（`介绍一下杭州`、`写一首关于秋天的诗`）模型会先写一大段推理，正文里甚至
有一句 `* Constraint: /no_think (This means I should no…`——**它看见了提示、权衡了一下，然后
照开不误**。`max_tokens` 一截，`</think>` 来不及吐，`ThinkStripper` 只能按"宁可露标签也不
静默吞内容"把整段推理原样发给客户端。

**修法**：模型自己的 `tokenizer.chat_template` 里，`add_generation_prompt` 关思考时本来就会补
`<think>\n\n</think>\n\n`——补了它模型就是从 `</think>` 后面开始生成的，**结构上开不了 think 段**。
是我们在 `toolcalls.py` 主动把这个尾巴去掉了（理由是"改它会让之前所有吞吐数字失去可比性"），
现在补回来（`rkllm_gateway.THINK_OFF`）。

- ⚠️ **关思考下的 prompt 长度与 `completion_tokens` 都变了**：那 4 个 token 以前是模型**生成**
  出来又被摘掉的，现在是预填。**2026-09-18 之前量到的吞吐数字不能与之后的逐字节互比。**
- 补完之后关思考下与模板**逐字节一致**，`toolcalls.py` 顶部记的两处"刻意偏差"就此消掉，
  `known`（KV 记账）从"近似"变成精确。
- 回归测试在 `nothink_check.py` 第 4 节（用 256 而不是 96 当上限——96 下 think 段必然非空，
  测到的就只是"被截断了"）；`check_template.py` 多了一条"第二轮 prompt 以「第一轮 + 回复」
  为前缀"——那才是网关真正用的 KV 复用判据。
- **板上验收（2026-09-18）**：四路并发流式复现（`enable_thinking=false`、256 token，与网页
  同形态）**0/4 带 `<think>`**（改前 3/4）；`介绍一下杭州。` 直接回正文；全套件 8 段 rc=0、
  FAIL 0、非零 rc 0、traceback 0（伸缩 10.95 / 20.08(1.83×) / 37.65(3.44×) tok/s）；
  `page_check.js` 对真板卡 + 真模型全绿。关思考的 KV 复用照常（第二轮 base=258 / sent=132 /
  full=390）。⚠️ 改的是**网关进程**，必须重启（模型重载 ~240s）；旧件留
  `serve/bak_20260918_thinkoff/`。`0d3e22c`

### 🆕 演示页可以自己设输出上限 · `9804a41`

原先写死 256，而网关与后端都没这个数（终端版能从命令行给到 2000+）。页头加输入框，
值记在 `localStorage`，空/非数字/≤0 一律回落默认值（漏成 0 会被网关读成"用后端默认"，
屏幕上就成了假话）。方案文档 §9.12。

### 🐞 演示页统计行把 HTML 当纯文本渲了 · `8bba09e`

`setStat()` 写的是 `textContent`，而每个调用方都传 HTML（要让速率加粗）——于是页面上直接印出
字面的 `<b>11.63 tok/s</b>`。看起来只是"没那么粗"、不像坏了，藏了好几轮演示。顺手补了 `esc()`：
失败分支拼的是网关回的错误文本。方案文档 §9.12.4。

## 2026-09-17

### 🆕 板上原生构建脚本入库 · `dfb8a99`

### 🐞 超过帧上限的载荷会把后端子进程打死 · `a54b259`（前一步 `d000d59`）

一个 3.4 MB 的请求就能让后端退出 → 四个会话全 dead → 之后每个请求在队列里干等到
`QUEUE_TIMEOUT` 才收到 503。改成**拒收**这一帧。（相关但不同的一条：1.6 MB 的请求体会让
**会话**成批死掉，成因仍未查清，见方案文档的已知限制。）

### 🆕 函数调用（Phase 1）：实现 + 板上验收 · `c1fd763` → `7b8f8a5`（文档 `c2c6f59`）

格式**不是猜的**，取自模型自己的 `tokenizer.chat_template`（XML，不是 Hermes JSON）。三处反直觉
的渲染规则见 `toolcalls.py` 顶部。**桩上全绿、板上第一遍就抓到一条真 FAIL**：回显助手轮丢了
`tool_calls`，而网关的前缀判据是**全有或全无** → 静默整段重算 469/469，不报错、答案也对。
方案文档 §9.9。

### 🐞 `restart_gateway.sh` 会杀掉执行它的那个 shell · `2345cf7`

`pkill -f` 的模式里带着二进制名，而 `INSTALL_DIR` 路径里也有那段字。现场像"程序自己崩了"。

### 🐞 prefill 分块按 `--bucket-size` 算而不是按输出张量 · `b13bbdb`

默认值下两种记法恰好相等，所以整套板卡测试全绿也证明不了它；板上复现要把 bucket 改成非 128
并喂一段跨分块的长 prompt。方案文档 §9.10。

## 2026-09-16

### 🐞 上下文将满时静默清 KV · `1dde346`

改成显式拒这一轮（REJECT 帧）：后端拒过的那一轮一个 token 都没进 KV，会话**没死**，不能标死。
⚠️ 新后端会发 REJECT 帧，**旧网关的读线程不认这个 tag、不消费 payload，整条流错位**——
后端与网关是一对，不能只换一半。

### 🐞 一帧 ERR 会打死整个网关 · `5c167f5`

ERR 被按 DELTA 解析 → 读线程死 → 之后全 503。与上一条是同一个"未知 tag 不消费"的机制。

## 2026-09-15

### 🆕 多用户边缘服务模式：显式身份、排队不抢占、主动交还 · `bc7f013`（tag `m5-multiuser`）

身份靠 `conversation_id` / `X-Conversation-Id` / `user`；**满了只排队不抢占**；离开要
`POST /v1/conversations/close`（否则占着一个 `IDLE_TTL`）。方案文档 §9.8。

### 🐞 会话池把已绑定的会话派给了新对话（坑 4）· `213a131`

⚠️ **修这个之前，任何单轮伸缩数字都不可引用**——同一个构建量到过 3.40× 与 1.00×。
诊断看网关日志的**租约落点**。方案文档 §4.4 口径 D。

### 🆕 演示页从网关自己发 · `570c406`，演示手册与终端版演示件 · `462cec8`

页面必须从 `http://<板卡IP>:8080/demo` 打开，双击本地文件会被跨域策略挡掉。
`_static_demo` 每次请求现读磁盘 → **换页面不用重启网关**。

## 2026-09-14 前后

### 🆕 `--serve` 帧协议 + 板端 OpenAI 兼容网关 · `6d0666a`（tag `m5-serve`）

多会话并发执行器 + 一套文本帧协议，网关把 OpenAI 的 `/v1/chat/completions` 翻译成它。
`--serve-fd` 实测落在 5（写死 3 会当场失效）。

---

## 引用这些数字之前

- 上面标 ⚠️ 的每一条都可能让**之前**量到的数字失效。方案文档里每张表都带口径与构建标记，
  引用时带上它，别只抄数字。
- 结论与明细表若不一致，以明细表为准；**口径不同不算矛盾**（例：手册里 200 token 口径的 3.48×
  与 §9.7 的 3.33×）。
